{
  "name": "hashdo-v2",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo-cards/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/cli"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
